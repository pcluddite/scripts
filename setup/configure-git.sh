#!/bin/sh

git config --global core.editor 'vim'

git config --global user.name 'Tim Baxendale'
git config --global user.email 'pcluddite@outlook.com'

git config --global pull.rebase true
git config --global rebase.autoStash true

